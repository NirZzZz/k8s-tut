apiVersion: v1
kind: Service
metadata:
  name: superset-service
spec:
  selector:
    app: superset
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8088
    nodePort: 30080  # Optional: Specify a port or let Kubernetes choose one
  type: NodePort
